[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Ensure environment variables are available
[build.environment]
  NODE_VERSION = "20"

# Make sure functions have access to environment variables
[functions]
  node_bundler = "esbuild"
